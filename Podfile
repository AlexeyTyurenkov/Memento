# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Graphmetrino' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  pod 'OpenCV'
  # Pods for Graphmetrino

  target 'GraphmetrinoTests' do
    inherit! :search_paths
    pod 'OpenCV'
    # Pods for testing
  end

  target 'GraphmetrinoUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
